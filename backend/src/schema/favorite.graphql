type Favorite {
  id: Int!
  characterId: Int!
  userId: Int!
  createdAt: String!
}

type ToggleFavoriteResult {
  added: Boolean!
}

extend type Mutation {
  toggleFavorite(characterId: Int!, userId: Int!): ToggleFavoriteResult!
}
